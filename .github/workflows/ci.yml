name: CI

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build-and-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Build unified cow pack
        run: ./scripts/build-mcaddon.sh

      - name: MCTools cooperative add-on validation (optional)
        run: ./scripts/validate_mctools.sh
        continue-on-error: true

      - name: Autoresearch backpressure checks
        run: ./.auto/checks.sh

      - name: Upload distributables
        uses: actions/upload-artifact@v4
        with:
          name: brindal-grayson-cow-pack
          path: |
            dist/brindal-grayson-cow-pack.mcaddon
            dist/brindal-grayson-cow-pack.mcpack
          retention-days: 30
